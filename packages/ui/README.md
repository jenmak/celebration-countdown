# @celebrationcountdown/ui

Storybook design-system package for Celebration Countdown.

```bash
npm run storybook -w @celebrationcountdown/ui
```

Add components under `src/<ComponentName>/` with co-located styles and stories. Export them from `src/index.ts`.
